import React from 'react';
import {Animated, Text} from 'react-native';
  
  
export default NopeComponent = (props) => {
    return (
  <Animated.View style={props.style.nopeAnimation}>
   <Text style={props.style.Nope}>NOPE</Text>

    </Animated.View> )}